import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#fb923c",
      main: "#ff6319",
      dark: "#ef4d00",
      contrastText: "#fff",
    },
  },
});

export default theme;
